<UserControl x:Class="CityOrganisations.CustomControls.CustomInformationControl.InformControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CityOrganisations.CustomControls.CustomInformationControl"
             x:Name="Root"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="155"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Label 
            Margin="0 0 15 0" 
            Content="{Binding Path=Label, RelativeSource={RelativeSource AncestorType=local:InformControlView}}"></Label>
        <TextBox Grid.Column="1"
                 TextAlignment="Left" 
                 VerticalAlignment="Center" 
                 Text="{Binding Path=BoxText, RelativeSource={RelativeSource AncestorType=local:InformControlView}}"></TextBox>
    </Grid>
</UserControl>
